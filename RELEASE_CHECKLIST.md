# 🚀 Чеклист подготовки к релизу

## ✅ Завершено

### 1. Очистка проекта
- ✅ Удалены все файлы документации разработки
- ✅ Удалены тестовые директории (tests, playwright, etc)
- ✅ Удалены ненужные конфигурационные файлы
- ✅ Обновлен README.md для production
- ✅ Очищен package.json от неиспользуемых скриптов
- ✅ Обновлен .gitignore для production

### 2. Настройка суперадминистратора
- ✅ Логин: `Сергей`
- ✅ Пароль: `qopdota2`
- ✅ Роль: `superadmin`
- ✅ Обновлен password hash
- ✅ Email: sergey@elclasico.ru

### 3. Мобильная оптимизация
- ✅ Гамбургер меню для навигации
- ✅ Touch-friendly кнопки (минимум 44x44px)
- ✅ Адаптивная типографика
- ✅ Горизонтальная прокрутка таблиц
- ✅ Оптимизированные формы

### 4. Конфигурация сервера
- ✅ ecosystem.config.json (PM2)
- ✅ nginx.conf
- ✅ deploy.sh
- ✅ update.sh
- ✅ .env.production

## 📋 Что осталось сделать вручную на сервере

### 1. Настройка домена
```bash
# В DNS панели Reg.ru
# Добавить A запись:
# yesfootboll.online → 176.125.254.82
# www.yesfootboll.online → 176.125.254.82
```

### 2. Установка SSL сертификата
```bash
# После настройки домена
sudo certbot --nginx -d yesfootboll.online -d www.yesfootboll.online
```

### 3. Обновление nginx.conf на сервере
```bash
# Обновить server_name в /etc/nginx/sites-available/elclasico
server_name yesfootboll.online www.yesfootboll.online;
```

### 4. Перезапуск сервисов
```bash
sudo nginx -t
sudo systemctl reload nginx
pm2 restart elclasico
```

## 🎯 Доступы для администратора

**Вход в админ-панель**: http://176.125.254.82/admin

**Учетные данные**:
- Логин: `Сергей`
- Пароль: `qopdota2`

## 📁 Финальная структура проекта

```
footbol-site/
├── .env.local              # Локальные переменные окружения
├── .env.production         # Production переменные
├── .gitignore             # Git ignore файл
├── app/                   # Next.js App Router
├── components/            # React компоненты
├── data/                  # JSON база данных
│   └── elclasico.json    # Пользователи, матчи, настройки
├── deploy.sh             # Скрипт автоматической установки
├── ecosystem.config.json # PM2 конфигурация
├── lib/                  # Утилиты
├── nginx.conf            # Nginx конфигурация
├── package.json          # NPM зависимости
├── prepare-deploy.sh     # Скрипт подготовки к деплою
├── README.md             # Документация проекта
├── store/                # Zustand стейт менеджмент
├── types/                # TypeScript типы
└── update.sh             # Скрипт быстрого обновления
```

## 🔄 Процесс обновления на сервере

1. Подключиться к серверу через SSH
2. Перейти в директорию проекта: `cd /var/www/footbol-site`
3. Запустить обновление: `./update.sh`

Или вручную:
```bash
git pull origin main
npm install
npm run build
pm2 restart elclasico
```

## 📞 Поддержка

При возникновении проблем проверьте:
- Логи PM2: `pm2 logs elclasico`
- Логи Nginx: `sudo tail -f /var/log/nginx/error.log`
- Статус сервисов: `pm2 status` и `sudo systemctl status nginx`

---

**Статус**: ✅ ГОТОВ К РЕЛИЗУ
**Дата**: 01.11.2025
**Версия**: 1.0.0
