# 🚀 Деплой на GitHub и VPS

## Часть 1: Публикация на GitHub

### Шаг 1: Установите Git (если еще не установлен)

Скачайте и установите Git для Windows:
https://git-scm.com/download/win

### Шаг 2: Инициализируйте репозиторий

```bash
# Откройте PowerShell в папке проекта
cd "C:\Users\KiskinDom\Desktop\footbol site"

# Инициализируйте Git
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "🎉 Initial commit: Amateur El Clasico v1.0"
```

### Шаг 3: Создайте репозиторий на GitHub

1. Перейдите на https://github.com
2. Нажмите "New repository"
3. Название: `amateur-el-clasico`
4. Описание: "⚽ Официальный сайт любительских команд Реал Мадрид vs Барселона"
5. Выберите: **Public** (или Private, если нужно)
6. **НЕ** создавайте README, .gitignore (у нас уже есть)
7. Нажмите "Create repository"

### Шаг 4: Загрузите код на GitHub

```bash
# Добавьте удаленный репозиторий (замените YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/amateur-el-clasico.git

# Переименуйте ветку в main
git branch -M main

# Загрузите код
git push -u origin main
```

### Шаг 5: Проверьте

Обновите страницу на GitHub - ваш код должен появиться!

---

## Часть 2: Деплой на VPS сервер

### Предварительные требования

- Ubuntu 22.04 VPS
- IP адрес: `176.125.254.82`
- SSH доступ
- Домен: `yesfootboll.online` (настроен)

### Шаг 1: Подключитесь к серверу

```bash
ssh root@176.125.254.82
```

### Шаг 2: Установите зависимости

```bash
# Обновите систему
apt update && apt upgrade -y

# Установите Node.js 18
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs

# Установите PM2 глобально
npm install -g pm2

# Установите Nginx
apt install -y nginx

# Установите Git
apt install -y git
```

### Шаг 3: Клонируйте репозиторий

```bash
# Создайте директорию для проекта
mkdir -p /var/www
cd /var/www

# Клонируйте с GitHub
git clone https://github.com/YOUR_USERNAME/amateur-el-clasico.git
cd amateur-el-clasico

# Установите зависимости
npm install

# Создайте production build
npm run build
```

### Шаг 4: Настройте PM2

```bash
# Запустите приложение через PM2
pm2 start ecosystem.config.json

# Настройте автозапуск
pm2 startup
pm2 save

# Проверьте статус
pm2 status
pm2 logs
```

### Шаг 5: Настройте Nginx

```bash
# Скопируйте конфигурацию Nginx
cp nginx.conf /etc/nginx/sites-available/elclasico

# Создайте символическую ссылку
ln -s /etc/nginx/sites-available/elclasico /etc/nginx/sites-enabled/

# Удалите дефолтный конфиг
rm /etc/nginx/sites-enabled/default

# Проверьте конфигурацию
nginx -t

# Перезапустите Nginx
systemctl restart nginx
```

### Шаг 6: Настройте SSL (Let's Encrypt)

```bash
# Установите Certbot
apt install -y certbot python3-certbot-nginx

# Получите SSL сертификат
certbot --nginx -d yesfootboll.online -d www.yesfootboll.online

# Автопродление сертификата
certbot renew --dry-run
```

### Шаг 7: Настройте Firewall

```bash
# Разрешите HTTP, HTTPS, SSH
ufw allow 22
ufw allow 80
ufw allow 443
ufw enable

# Проверьте статус
ufw status
```

---

## Часть 3: Обновление кода

### Вариант А: Автоматическое обновление (рекомендуется)

```bash
# На сервере запустите скрипт
cd /var/www/amateur-el-clasico
./update.sh
```

Скрипт `update.sh` делает:
1. Создает бэкап базы данных
2. Скачивает новый код с GitHub
3. Устанавливает зависимости
4. Делает новый build
5. Перезапускает PM2

### Вариант Б: Ручное обновление

```bash
cd /var/www/amateur-el-clasico

# Бэкап базы данных
cp data/elclasico.json data/elclasico.backup.json

# Скачайте новый код
git pull origin main

# Установите новые зависимости
npm install

# Пересоберите проект
npm run build

# Перезапустите приложение
pm2 restart amateur-el-clasico
```

---

## Часть 4: Мониторинг и обслуживание

### Просмотр логов

```bash
# Логи приложения
pm2 logs amateur-el-clasico

# Логи Nginx
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log

# Системные ресурсы
pm2 monit
htop
```

### Бэкапы базы данных

```bash
# Создать бэкап
cp /var/www/amateur-el-clasico/data/elclasico.json \
   /root/backups/elclasico-$(date +%Y%m%d-%H%M%S).json

# Автоматический бэкап (cron)
crontab -e

# Добавьте строку (бэкап каждый день в 3:00)
0 3 * * * cp /var/www/amateur-el-clasico/data/elclasico.json /root/backups/elclasico-$(date +\%Y\%m\%d).json
```

### Перезапуск сервисов

```bash
# Перезапуск приложения
pm2 restart amateur-el-clasico

# Перезапуск Nginx
systemctl restart nginx

# Полная перезагрузка сервера
reboot
```

---

## Часть 5: Настройка домена

### DNS записи (у регистратора домена)

```
Тип  | Имя              | Значение
-----|------------------|-------------------
A    | @                | 176.125.254.82
A    | www              | 176.125.254.82
```

### Проверка домена

```bash
# Проверьте DNS
nslookup yesfootboll.online
ping yesfootboll.online

# Проверьте сайт
curl -I https://yesfootboll.online
```

---

## 🆘 Решение проблем

### Сайт не открывается

```bash
# Проверьте статус PM2
pm2 status

# Проверьте порт 3000
netstat -tulpn | grep 3000

# Проверьте Nginx
systemctl status nginx
```

### Ошибка 502 Bad Gateway

```bash
# Перезапустите приложение
pm2 restart amateur-el-clasico

# Проверьте логи
pm2 logs --err
```

### База данных не обновляется

```bash
# Проверьте права доступа
ls -la /var/www/amateur-el-clasico/data/
chmod 644 /var/www/amateur-el-clasico/data/elclasico.json
```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `pm2 logs`
2. Прочитайте документацию
3. Создайте Issue на GitHub
4. Напишите на email: admin@yesfootboll.online

---

**Удачного деплоя! ⚽🚀**
