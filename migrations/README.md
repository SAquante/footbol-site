# Миграции базы данных

## Применение миграций

### Windows (PowerShell)
```powershell
# Перейдите в папку с проектом
cd "c:\Users\KiskinDom\Desktop\footbol site"

# Примените миграцию добавления поля announcement
psql -U postgres -d elclasico -f migrations/add_announcement.sql
```

### Linux/Mac
```bash
# Перейдите в папку с проектом
cd ~/footbol-site

# Примените миграцию
psql -U postgres -d elclasico -f migrations/add_announcement.sql
```

## Важно!

После применения миграции `add_announcement.sql` у вас появится новое поле "Анонс" в форме создания/редактирования матчей.

## Учетные данные суперадмина

**Логин:** Сергей  
**Пароль:** qopdota2  
**Роль:** superadmin

## Что добавлено в версии с полем "Анонс"

1. **Новое поле в базе данных:** `announcement` (TEXT, nullable)
2. **Форма создания матча:** Добавлено текстовое поле "📢 Анонс матча"
3. **Форма редактирования:** Аналогичное поле с сохранением существующих данных
4. **Главная страница:** Анонс отображается под составами команд в красивом блоке
5. **API обновлено:** POST /api/matches и PUT /api/matches/[id] поддерживают поле announcement
6. **TypeScript типы:** Обновлены интерфейсы Match и UpdateMatchData

## Мобильный дизайн

Все элементы адаптированы под мобильные устройства:
- Анонс отображается с адаптивными отступами (p-4 на мобильных, p-6 на десктопе)
- Текст масштабируется (text-sm на мобильных, text-base на десктопе)
- Заголовок анонса адаптивный (text-lg на мобильных, text-xl на десктопе)
