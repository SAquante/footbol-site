# 📍 Google Maps Integration - Quick Start

## ✅ Что было сделано:

1. **Установлены пакеты:**
   - `@react-google-maps/api` - компоненты React для Google Maps
   - `@types/google.maps` - типы TypeScript

2. **Создан компонент `LocationPicker`:**
   - Интерактивная карта с маркером
   - Поиск адресов через автозаполнение (Google Places Autocomplete)
   - Выбор места кликом на карте
   - Обратное геокодирование (координаты → адрес)

3. **Интегрировано в формы:**
   - `/admin/create` - создание нового матча
   - `/admin/edit/[id]` - редактирование существующего матча

4. **Адрес по умолчанию:**
   - ул. Константина Заслонова, 23 корпус 4, Санкт-Петербург, Россия, 191119
   - Координаты: 59.9386, 30.3606

## 🚀 Запуск

### Шаг 1: Получите Google Maps API ключ

Следуйте инструкциям в файле `GOOGLE_MAPS_SETUP.md`

### Шаг 2: Добавьте ключ в `.env.local`

```bash
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=ваш_ключ_здесь
```

### Шаг 3: Перезапустите сервер

```bash
npm run dev
```

## 💡 Как использовать

### В форме создания/редактирования матча:

1. **Поиск по адресу:**
   - Начните вводить адрес в поле поиска
   - Выберите нужный адрес из списка подсказок
   - Карта автоматически переместится к выбранному месту

2. **Выбор на карте:**
   - Кликните на любое место на карте
   - Система автоматически определит адрес по координатам
   - Адрес обновится в поле

3. **Выбранный адрес:**
   - Отображается под картой
   - Сохраняется при создании/редактировании матча

## 🎨 Функции

- ✅ Автозаполнение адресов (только Россия)
- ✅ Интерактивная карта
- ✅ Перетаскиваемый маркер
- ✅ Геокодирование (адрес ↔ координаты)
- ✅ Адрес по умолчанию
- ✅ Responsive дизайн
- ✅ Темная тема

## 🔧 Настройки

Вы можете изменить адрес по умолчанию в компоненте:

`components/LocationPicker.tsx`:
```typescript
const defaultCenter = {
  lat: 59.9386, // ваши координаты
  lng: 30.3606
};

const defaultAddress = "ваш адрес";
```

## 📝 Примечания

- Карта загружается только при открытии формы создания/редактирования
- API ключ должен быть настроен для работы поиска
- Без ключа карта не будет отображаться
- Поиск ограничен территорией России (`componentRestrictions: { country: 'ru' }`)

## 🐛 Troubleshooting

**Карта не отображается:**
- Проверьте правильность API ключа в `.env.local`
- Убедитесь, что активированы необходимые API (Maps JavaScript API, Places API, Geocoding API)
- Перезапустите dev сервер после изменения `.env.local`

**Поиск не работает:**
- Проверьте, что Places API активирован в Google Cloud Console
- Проверьте квоты API в консоли

**Геокодирование не работает:**
- Проверьте, что Geocoding API активирован
- Проверьте лимиты бесплатного использования
