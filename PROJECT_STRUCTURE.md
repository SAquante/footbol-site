# 📊 Структура и возможности проекта

## 🏗️ Полная структура файлов

```
footbol site/
│
├── 📄 Конфигурационные файлы
│   ├── package.json              # Зависимости и скрипты
│   ├── tsconfig.json            # Настройки TypeScript
│   ├── tailwind.config.js       # Конфигурация Tailwind CSS
│   ├── postcss.config.js        # PostCSS конфигурация
│   ├── next.config.js           # Настройки Next.js
│   ├── .env.local               # Переменные окружения
│   └── .gitignore               # Git ignore правила
│
├── 📚 Документация
│   ├── README.md                # Полная документация
│   ├── START_HERE.md            # Начните отсюда!
│   ├── QUICKSTART.md            # Быстрый старт
│   ├── API_EXAMPLES.md          # Примеры API
│   └── PROJECT_STRUCTURE.md     # Этот файл
│
├── 📁 app/                      # Next.js App Router
│   ├── layout.tsx               # Корневой layout
│   ├── page.tsx                 # Главная страница
│   ├── globals.css              # Глобальные стили
│   │
│   ├── 🔐 api/                  # API Routes
│   │   ├── auth/
│   │   │   ├── login/route.ts       # POST /api/auth/login
│   │   │   └── register/route.ts    # POST /api/auth/register
│   │   └── matches/
│   │       ├── route.ts             # GET, POST /api/matches
│   │       └── [id]/route.ts        # GET, PUT, DELETE /api/matches/:id
│   │
│   ├── 👤 login/                # Страница входа
│   │   └── page.tsx
│   ├── 📝 register/             # Страница регистрации
│   │   └── page.tsx
│   ├── 📅 schedule/             # Расписание матчей
│   │   └── page.tsx
│   ├── ⚽ match/[id]/           # Детали матча
│   │   └── page.tsx
│   │
│   └── 🛡️ admin/               # Админ-панель
│       ├── page.tsx                 # Список матчей
│       ├── create/page.tsx          # Создание матча
│       └── edit/[id]/page.tsx       # Редактирование матча
│
├── 🧩 components/               # React компоненты
│   ├── Navigation.tsx           # Навигационное меню
│   ├── CountdownTimer.tsx       # Таймер обратного отсчета
│   └── MatchCard.tsx            # Карточка матча
│
├── 💾 database/                 # База данных
│   └── schema.sql               # SQL схема таблиц
│
├── 🛠️ lib/                     # Утилиты и библиотеки
│   ├── db.ts                    # Подключение к PostgreSQL
│   └── auth.ts                  # JWT функции
│
├── 📦 store/                    # State Management
│   └── authStore.ts             # Zustand store для аутентификации
│
├── 🏷️ types/                   # TypeScript типы
│   └── index.ts                 # Интерфейсы и типы
│
└── 📜 scripts/                  # Скрипты
    └── init-db.js               # Инициализация БД
```

---

## 🎯 Основные страницы

### 🏠 Публичные страницы

| Страница | Путь | Описание |
|----------|------|----------|
| Главная | `/` | Следующий матч, таймер, составы, последние игры |
| Расписание | `/schedule` | Все предстоящие и прошедшие матчи |
| Детали матча | `/match/:id` | Полная информация о конкретном матче |
| Вход | `/login` | Форма входа в систему |
| Регистрация | `/register` | Регистрация нового пользователя |

### 🛡️ Админ-панель (требует ADMIN роль)

| Страница | Путь | Описание |
|----------|------|----------|
| Админ-панель | `/admin` | Список всех матчей с управлением |
| Создать матч | `/admin/create` | Форма создания нового матча |
| Редактировать | `/admin/edit/:id` | Редактирование матча, составов, счета |

---

## 🔌 API Endpoints

### Аутентификация

```
POST   /api/auth/register    - Регистрация нового пользователя
POST   /api/auth/login       - Вход в систему (получение JWT токена)
```

### Матчи

```
GET    /api/matches          - Получить все матчи
POST   /api/matches          - Создать новый матч (ADMIN)
GET    /api/matches/:id      - Получить конкретный матч
PUT    /api/matches/:id      - Обновить матч (ADMIN)
DELETE /api/matches/:id      - Удалить матч (ADMIN)
```

---

## 🎨 Дизайн система

### Цветовая палитра

```javascript
// Темная основа
'dark-primary': '#000000'      // Основной фон
'dark-secondary': '#111111'    // Карточки, блоки
'dark-accent': '#1a1a1a'       // Инпуты, акценты

// Реал Мадрид
'real-gold': '#FFD700'         // Золотой
'real-white': '#FFFFFF'        // Белый

// Барселона
'barca-blue': '#004D98'        // Синий
'barca-red': '#DC0028'         // Красный

// Неоновые акценты
'neon-blue': '#00F0FF'         // Голубой неон
'neon-red': '#FF003C'          // Красный неон
```

### Кастомные классы (Tailwind)

```css
/* Кнопки */
.btn-primary       - Базовая кнопка
.btn-real          - Кнопка в стиле Реала (золотая)
.btn-barca         - Кнопка в стиле Барсы (сине-красная)

/* Карточки */
.card              - Базовая карточка
.card-hover        - Карточка с hover эффектом

/* Поля ввода */
.input-field       - Стилизованное поле ввода

/* Градиенты текста */
.text-gradient-real   - Градиент Реала
.text-gradient-barca  - Градиент Барсы

/* VS эффект */
.vs-divider        - Разделитель с VS
.vs-text           - Анимированный текст VS
```

---

## 🗄️ Схема базы данных

### Таблица `users`

```sql
id             SERIAL PRIMARY KEY
username       VARCHAR(255) UNIQUE NOT NULL
password_hash  VARCHAR(255) NOT NULL
role           VARCHAR(50) DEFAULT 'PLAYER'  -- 'ADMIN' или 'PLAYER'
created_at     TIMESTAMP DEFAULT NOW()
```

### Таблица `matches`

```sql
id              SERIAL PRIMARY KEY
match_datetime  TIMESTAMP NOT NULL
location        VARCHAR(255) NOT NULL
status          VARCHAR(50) DEFAULT 'scheduled'  -- 'scheduled' или 'completed'
score_real      INTEGER NULL
score_barca     INTEGER NULL
lineup_real     TEXT NULL
lineup_barca    TEXT NULL
created_at      TIMESTAMP DEFAULT NOW()
updated_at      TIMESTAMP DEFAULT NOW()
```

---

## ⚙️ Технологии и библиотеки

### Frontend

- **Next.js 14.0.4** - React фреймворк
- **React 18.2.0** - UI библиотека
- **TypeScript 5.3.3** - Типизация
- **Tailwind CSS 3.4.0** - Утилитарный CSS
- **Framer Motion 10.16.16** - Анимации
- **date-fns 3.0.6** - Работа с датами
- **React Hook Form 7.49.2** - Формы
- **Zustand 4.4.7** - State management

### Backend

- **Node.js** - Runtime
- **PostgreSQL** - База данных
- **pg 8.11.3** - PostgreSQL драйвер
- **bcryptjs 2.4.3** - Хэширование паролей
- **jsonwebtoken 9.0.2** - JWT токены

---

## 🔒 Безопасность

### Аутентификация
- Пароли хэшируются с помощью **bcrypt** (10 раундов)
- JWT токены с временем жизни **7 дней**
- Защищенные API маршруты требуют Authorization header

### Роли пользователей
- **ADMIN** - полный доступ к админ-панели и управлению
- **PLAYER** - обычный пользователь (может видеть расписание)

### Переменные окружения
```env
DATABASE_URL       # Строка подключения к PostgreSQL
JWT_SECRET         # Секретный ключ для JWT
NEXTAUTH_SECRET    # Секрет для NextAuth
```

---

## 📱 Адаптивность

Проект полностью адаптирован для:

- 📱 **Мобильные телефоны** (320px+)
- 📱 **Планшеты** (768px+)
- 💻 **Десктоп** (1024px+)
- 🖥️ **Широкие экраны** (1920px+)

### Breakpoints Tailwind:
```
sm:  640px   - Мобильные (landscape)
md:  768px   - Планшеты
lg:  1024px  - Маленькие ноутбуки
xl:  1280px  - Десктоп
2xl: 1536px  - Широкие экраны
```

---

## 🚀 Производительность

### Оптимизации Next.js:
- ⚡ Server-Side Rendering (SSR)
- 🔄 Automatic Static Optimization
- 📦 Code Splitting
- 🖼️ Image Optimization
- 🗜️ Automatic Compression

### Стратегия загрузки:
- Критичные стили - inline
- JavaScript - split chunks
- Шрифты - preload
- API данные - на уровне компонентов

---

## 🎭 Анимации (Framer Motion)

### Используемые эффекты:

```javascript
// Fade in (появление)
initial={{ opacity: 0 }}
animate={{ opacity: 1 }}

// Slide up (выезд снизу)
initial={{ opacity: 0, y: 20 }}
animate={{ opacity: 1, y: 0 }}

// Slide from sides (с боков)
initial={{ opacity: 0, x: -30 }}  // слева
initial={{ opacity: 0, x: 30 }}   // справа

// Hover effects
whileHover={{ scale: 1.05 }}
```

---

## 📊 State Management

### Zustand Store (authStore)

```typescript
interface AuthState {
  user: User | null
  token: string | null
  setAuth: (user, token) => void
  logout: () => void
}
```

Данные сохраняются в **localStorage** автоматически.

---

## 🌐 Интернационализация

Проект полностью **русифицирован**:

- ✅ Все UI элементы на русском
- ✅ Даты форматируются по русской локали
- ✅ Сообщения об ошибках на русском
- ✅ Документация на русском

---

## 🔧 Полезные команды

```bash
# Разработка
npm run dev              # Запуск dev сервера
npm run build            # Production сборка
npm start                # Запуск production
npm run lint             # Проверка кода

# База данных
npm run db:init          # Инициализация БД

# Установка
npm install              # Установить зависимости
npm ci                   # Чистая установка (CI/CD)
```

---

## 📈 Возможные расширения

### Идеи для будущего:

1. **Статистика игроков**
   - Количество голов
   - Количество игр
   - MVP матча

2. **Фото матчей**
   - Загрузка фото
   - Галерея

3. **Комментарии**
   - Обсуждение матчей
   - Реакции

4. **Уведомления**
   - Email напоминания
   - Push уведомления

5. **Турнирная таблица**
   - Счет сезона
   - Статистика команд

6. **Экспорт данных**
   - PDF с расписанием
   - Excel с результатами

---

<div align="center">

**Структура проекта "Amateur El Clásico"**

Создано с использованием современных технологий веб-разработки

</div>
