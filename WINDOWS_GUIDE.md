# 📦 Инструкция для Windows: Подготовка к загрузке на сервер

## Вариант 1: Загрузка через FileZilla (Проще всего)

### Шаг 1: Скачайте FileZilla Client
Скачайте с официального сайта: https://filezilla-project.org/download.php?type=client

### Шаг 2: Подключитесь к серверу
1. Откройте FileZilla
2. Заполните поля вверху:
   - **Хост:** `sftp://IP_вашего_VPS` (IP вы получили от Reg.ru)
   - **Имя пользователя:** `root`
   - **Пароль:** ваш пароль от VPS
   - **Порт:** `22`
3. Нажмите "Быстрое соединение"

### Шаг 3: Загрузите файлы
1. Слева - ваш компьютер, найдите папку `c:\Users\я\Desktop\footbol site`
2. Справа - сервер, перейдите в `/var/www/footbol-site`
3. Выберите ВСЕ файлы слева и перетащите вправо

**НЕ загружайте:**
- `node_modules` (будет установлено на сервере)
- `.next` (будет создано при сборке)

---

## Вариант 2: Через GitHub (Рекомендуется для обновлений)

### Шаг 1: Создайте репозиторий на GitHub
1. Перейдите на https://github.com
2. Нажмите "New repository"
3. Название: `footbol-site`
4. Нажмите "Create repository"

### Шаг 2: Загрузите проект на GitHub
Откройте PowerShell в папке проекта:

```powershell
cd "c:\Users\я\Desktop\footbol site"

# Инициализируйте Git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit"

# Добавьте ссылку на GitHub (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/footbol-site.git

# Загрузите на GitHub
git branch -M main
git push -u origin main
```

### Шаг 3: На сервере склонируйте репозиторий
```bash
cd /var/www/footbol-site
git clone https://github.com/YOUR_USERNAME/footbol-site.git .
```

---

## Вариант 3: Создать архив и загрузить

### В Windows:
```powershell
# Перейдите в папку проекта
cd "c:\Users\я\Desktop"

# Создайте архив (нужен 7-Zip или WinRAR)
# Исключите node_modules и .next
```

Или вручную:
1. Выделите все файлы КРОМЕ `node_modules` и `.next`
2. Правая кнопка -> "Отправить" -> "Сжатая ZIP-папка"
3. Загрузите через FileZilla
4. На сервере распакуйте: `unzip footbol-site.zip`

---

## ✅ Что должно быть загружено на сервер:

- ✅ `/app` - все страницы
- ✅ `/components` - компоненты
- ✅ `/data` - данные
- ✅ `/lib` - утилиты
- ✅ `/public` - статические файлы
- ✅ `/scripts` - скрипты
- ✅ `package.json` и `package-lock.json`
- ✅ `next.config.js`
- ✅ `tailwind.config.ts`
- ✅ `tsconfig.json`
- ✅ `.env.production`
- ✅ `ecosystem.config.json`
- ✅ `nginx.conf`
- ✅ `deploy.sh`
- ✅ `update.sh`

- ❌ `node_modules` - НЕ загружайте
- ❌ `.next` - НЕ загружайте
- ❌ `.env.local` - НЕ загружайте (используйте .env.production)

---

## 🚀 После загрузки файлов на сервер:

Подключитесь по SSH через PuTTY:
1. Скачайте PuTTY: https://www.putty.org/
2. Откройте PuTTY
3. Host Name: IP вашего VPS
4. Port: 22
5. Нажмите "Open"
6. Введите: `root` и пароль

Выполните команды:
```bash
cd /var/www/footbol-site
npm install
npm run build
pm2 start ecosystem.config.json
```

---

## 📞 Нужна помощь?

Если возникли сложности:
1. Проверьте, что VPS активирован в панели Reg.ru
2. Проверьте, что у вас есть SSH доступ
3. Используйте FileZilla - это самый простой способ
