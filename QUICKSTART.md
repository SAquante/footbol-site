# 🎯 Быстрый старт

## Шаг 1: Установка PostgreSQL

### Windows:
1. Скачайте PostgreSQL с [официального сайта](https://www.postgresql.org/download/windows/)
2. Запустите установщик
3. Запомните пароль для пользователя `postgres`
4. Порт по умолчанию: `5432`

### Проверка установки:
```powershell
psql --version
```

## Шаг 2: Создание базы данных

```powershell
# Подключитесь к PostgreSQL
psql -U postgres

# В консоли PostgreSQL выполните:
CREATE DATABASE elclasico;

# Проверьте, что БД создана:
\l

# Выйдите:
\q
```

## Шаг 3: Установка проекта

```powershell
# Установите зависимости
npm install

# Это может занять 2-3 минуты
```

## Шаг 4: Настройка .env.local

Откройте файл `.env.local` и измените строку подключения к БД:

```env
DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/elclasico
```

Замените `ВАШ_ПАРОЛЬ` на пароль, который вы указали при установке PostgreSQL.

## Шаг 5: Инициализация БД

```powershell
npm run db:init
```

Вы должны увидеть:
```
✅ База данных успешно инициализирована!
👤 Создан администратор: username = "admin", password = "admin123"
```

## Шаг 6: Запуск приложения

```powershell
npm run dev
```

Откройте браузер: [http://localhost:3000](http://://localhost:3000)

## 🎉 Готово!

### Первые шаги:

1. **Войдите как администратор**:
   - Логин: `admin`
   - Пароль: `admin123`

2. **Создайте первый матч**:
   - Нажмите "Админ-панель"
   - Кликните "Создать матч"
   - Заполните форму

3. **Добавьте составы**:
   - В админ-панели нажмите "Редактировать" на матче
   - Прокрутите до секции "Составы команд"
   - Введите имена игроков (каждый с новой строки)

4. **После игры внесите счет**:
   - Редактируйте матч
   - Измените статус на "Сыгран"
   - Введите итоговый счет

---

## ⚠️ Частые ошибки

### "ECONNREFUSED" при запуске

**Проблема**: PostgreSQL не запущен.

**Решение**:
```powershell
# Запустите PostgreSQL (от администратора)
net start postgresql-x64-14
```

Или откройте "Службы" (Services) и запустите PostgreSQL вручную.

### "Error: connect ETIMEDOUT"

**Проблема**: Неверные данные подключения в `.env.local`

**Решение**: Проверьте:
- Пароль PostgreSQL
- Имя базы данных (`elclasico`)
- Порт (обычно `5432`)

### "relation does not exist"

**Проблема**: БД не инициализирована.

**Решение**:
```powershell
npm run db:init
```

---

## 📱 Использование на телефоне

1. Узнайте IP вашего компьютера:
```powershell
ipconfig
```
Найдите "IPv4 Address" (например, `192.168.1.100`)

2. Откройте на телефоне:
```
http://192.168.1.100:3000
```

⚠️ **Важно**: Компьютер и телефон должны быть в одной Wi-Fi сети.

---

## 🔄 Обновление проекта

Если вы изменили код:

```powershell
# Остановите сервер (Ctrl+C)

# Переустановите зависимости (если изменился package.json)
npm install

# Запустите снова
npm run dev
```

---

## 💾 Backup базы данных

### Создание backup:
```powershell
pg_dump -U postgres -d elclasico > backup.sql
```

### Восстановление:
```powershell
psql -U postgres -d elclasico < backup.sql
```

---

## 🎨 Персонализация

### Изменить название команд:

1. Откройте любой файл с "Реал Мадрид" или "Барселона"
2. Используйте "Найти и заменить" (Ctrl+H) в VS Code
3. Замените на свои названия

### Изменить цвета:

Откройте `tailwind.config.js` и измените цвета в секции `colors`.

---

## 📞 Контакты

Если что-то не работает:
1. Перечитайте инструкцию
2. Проверьте все шаги
3. Убедитесь, что PostgreSQL запущен
4. Проверьте `.env.local` файл

---

**Удачи с вашими матчами! ⚽**
