# 🚀 Быстрый старт# 🎯 Быстрый старт



## Локальная разработка## Шаг 1: Установка PostgreSQL



```bash### Windows:

# Установка зависимостей1. Скачайте PostgreSQL с [официального сайта](https://www.postgresql.org/download/windows/)

npm install2. Запустите установщик

3. Запомните пароль для пользователя `postgres`

# Запуск dev сервера4. Порт по умолчанию: `5432`

npm run dev

### Проверка установки:

# Откройте http://localhost:3000```powershell

```psql --version

```

## Production сборка

## Шаг 2: Создание базы данных

```bash

# Сборка проекта```powershell

npm run build# Подключитесь к PostgreSQL

psql -U postgres

# Запуск production сервера

npm start# В консоли PostgreSQL выполните:

```CREATE DATABASE elclasico;



## Деплой на сервер# Проверьте, что БД создана:

\l

### Автоматический деплой

```bash# Выйдите:

# Скопируйте проект на сервер\q

scp -r * user@176.125.254.82:/var/www/footbol-site/```



# Подключитесь к серверу## Шаг 3: Установка проекта

ssh user@176.125.254.82

```powershell

# Перейдите в директорию# Установите зависимости

cd /var/www/footbol-sitenpm install



# Запустите скрипт деплоя# Это может занять 2-3 минуты

chmod +x deploy.sh```

./deploy.sh

```## Шаг 4: Настройка .env.local



### Быстрое обновлениеОткройте файл `.env.local` и измените строку подключения к БД:

```bash

# На сервере```env

cd /var/www/footbol-siteDATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/elclasico

./update.sh```

```

Замените `ВАШ_ПАРОЛЬ` на пароль, который вы указали при установке PostgreSQL.

## Управление PM2

## Шаг 5: Инициализация БД

```bash

# Статус```powershell

pm2 statusnpm run db:init

```

# Логи

pm2 logs elclasicoВы должны увидеть:

```

# Рестарт✅ База данных успешно инициализирована!

pm2 restart elclasico👤 Создан администратор: username = "admin", password = "admin123"

```

# Стоп

pm2 stop elclasico## Шаг 6: Запуск приложения



# Автозапуск при перезагрузке```powershell

pm2 startupnpm run dev

pm2 save```

```

Откройте браузер: [http://localhost:3000](http://://localhost:3000)

## Администрирование

## 🎉 Готово!

**Вход в админ-панель**: `/admin`

### Первые шаги:

**Суперадминистратор**:

- Логин: `Сергей`1. **Войдите как администратор**:

- Пароль: `qopdota2`   - Логин: `admin`

   - Пароль: `admin123`

## Полезные команды

2. **Создайте первый матч**:

```bash   - Нажмите "Админ-панель"

# Проверка Nginx конфигурации   - Кликните "Создать матч"

sudo nginx -t   - Заполните форму



# Перезагрузка Nginx3. **Добавьте составы**:

sudo systemctl reload nginx   - В админ-панели нажмите "Редактировать" на матче

   - Прокрутите до секции "Составы команд"

# Логи Nginx   - Введите имена игроков (каждый с новой строки)

sudo tail -f /var/log/nginx/error.log

sudo tail -f /var/log/nginx/access.log4. **После игры внесите счет**:

   - Редактируйте матч

# Проверка процессов Node.js   - Измените статус на "Сыгран"

ps aux | grep node   - Введите итоговый счет



# Проверка занятых портов---

sudo netstat -tulpn | grep :3000

```## ⚠️ Частые ошибки



## Настройка SSL (после настройки домена)### "ECONNREFUSED" при запуске



```bash**Проблема**: PostgreSQL не запущен.

# Установка Certbot

sudo apt install certbot python3-certbot-nginx**Решение**:

```powershell

# Получение сертификата# Запустите PostgreSQL (от администратора)

sudo certbot --nginx -d yesfootboll.online -d www.yesfootboll.onlinenet start postgresql-x64-14

```

# Автообновление сертификата (уже настроено автоматически)

sudo certbot renew --dry-runИли откройте "Службы" (Services) и запустите PostgreSQL вручную.

```

### "Error: connect ETIMEDOUT"

## Резервное копирование

**Проблема**: Неверные данные подключения в `.env.local`

```bash

# Бэкап базы данных**Решение**: Проверьте:

cp data/elclasico.json data/elclasico.json.backup- Пароль PostgreSQL

- Имя базы данных (`elclasico`)

# Бэкап с датой- Порт (обычно `5432`)

cp data/elclasico.json data/elclasico.$(date +%Y%m%d_%H%M%S).json

```### "relation does not exist"



## Решение проблем**Проблема**: БД не инициализирована.



### Сайт не открывается**Решение**:

1. Проверьте PM2: `pm2 status````powershell

2. Проверьте Nginx: `sudo systemctl status nginx`npm run db:init

3. Проверьте порт 3000: `sudo netstat -tulpn | grep :3000````



### Ошибки в логах---

```bash

# Посмотреть последние 50 строк логов## 📱 Использование на телефоне

pm2 logs elclasico --lines 50

1. Узнайте IP вашего компьютера:

# Очистить логи```powershell

pm2 flushipconfig

``````

Найдите "IPv4 Address" (например, `192.168.1.100`)

### Обновление не применилось

```bash2. Откройте на телефоне:

# Полная пересборка```

npm installhttp://192.168.1.100:3000

npm run build```

pm2 restart elclasico --update-env

```⚠️ **Важно**: Компьютер и телефон должны быть в одной Wi-Fi сети.



------



**Адрес сайта**: http://176.125.254.82  ## 🔄 Обновление проекта

**Будущий домен**: yesfootboll.online

Если вы изменили код:

```powershell
# Остановите сервер (Ctrl+C)

# Переустановите зависимости (если изменился package.json)
npm install

# Запустите снова
npm run dev
```

---

## 💾 Backup базы данных

### Создание backup:
```powershell
pg_dump -U postgres -d elclasico > backup.sql
```

### Восстановление:
```powershell
psql -U postgres -d elclasico < backup.sql
```

---

## 🎨 Персонализация

### Изменить название команд:

1. Откройте любой файл с "Реал Мадрид" или "Барселона"
2. Используйте "Найти и заменить" (Ctrl+H) в VS Code
3. Замените на свои названия

### Изменить цвета:

Откройте `tailwind.config.js` и измените цвета в секции `colors`.

---

## 📞 Контакты

Если что-то не работает:
1. Перечитайте инструкцию
2. Проверьте все шаги
3. Убедитесь, что PostgreSQL запущен
4. Проверьте `.env.local` файл

---

**Удачи с вашими матчами! ⚽**
