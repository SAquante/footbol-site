# 🗺️ Настройка Google Maps API

Для работы интеграции с Google Maps необходимо получить API ключ.

## Получение API ключа

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Перейдите в раздел "APIs & Services" → "Credentials"
4. Нажмите "Create Credentials" → "API key"
5. Скопируйте полученный ключ

## Активация необходимых API

В разделе "APIs & Services" → "Library" активируйте:
- **Maps JavaScript API**
- **Places API**
- **Geocoding API**

## Настройка ограничений (опционально, но рекомендуется)

1. В разделе "Credentials" нажмите на созданный API ключ
2. В "Application restrictions" выберите:
   - **HTTP referrers** для production
   - Добавьте: `http://localhost:3000/*` для разработки
   - Добавьте ваш production домен: `https://yourdomain.com/*`
3. В "API restrictions" выберите "Restrict key"
4. Отметьте:
   - Maps JavaScript API
   - Places API
   - Geocoding API

## Добавление ключа в проект

Откройте файл `.env.local` и замените `YOUR_GOOGLE_MAPS_API_KEY_HERE` на ваш ключ:

```bash
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSy...ваш_ключ_здесь
```

## Перезапуск сервера

После добавления ключа перезапустите dev сервер:

```bash
npm run dev
```

## Адрес по умолчанию

По умолчанию установлен адрес:
**ул. Константина Заслонова, 23 корпус 4, Санкт-Петербург, Россия, 191119**

Координаты: 59.9386, 30.3606

## Функциональность

✅ Поиск адресов через автозаполнение (только по России)
✅ Выбор места кликом на карте
✅ Автоматическое получение адреса по координатам (геокодирование)
✅ Интеграция в формы создания и редактирования матчей

## Бесплатный лимит

Google предоставляет $200 кредитов в месяц, этого достаточно для:
- ~28,000 загрузок карт
- ~40,000 геокодирований
- Неограниченных автозаполнений

Для небольших проектов этого более чем достаточно!
