# ⚠️ ВАЖНО! Перед деплоем на сервер

## 🔒 Безопасность

### 1. Смена паролей
Сейчас установлен тестовый пароль. После первого входа рекомендуется:
- Сменить пароль суперадминистратора
- Создать резервную копию базы данных

### 2. Резервное копирование
```bash
# На сервере создайте бэкап перед деплоем
cp data/elclasico.json data/elclasico.backup.json

# Или с датой
cp data/elclasico.json data/elclasico.$(date +%Y%m%d).json
```

### 3. Права доступа к файлам
```bash
# Убедитесь что права настроены правильно
chmod 755 deploy.sh update.sh prepare-deploy.sh
chmod 644 data/elclasico.json
```

## 🌐 Настройка DNS (обязательно!)

После настройки DNS обновите nginx конфигурацию:

```nginx
# В /etc/nginx/sites-available/elclasico
server {
    listen 80;
    server_name yesfootboll.online www.yesfootboll.online;
    # ... остальная конфигурация
}
```

## 🔐 SSL сертификат

**Важно**: SSL можно установить только ПОСЛЕ настройки DNS!

```bash
# Установка Certbot (если еще не установлен)
sudo apt install certbot python3-certbot-nginx

# Получение сертификата
sudo certbot --nginx -d yesfootboll.online -d www.yesfootboll.online
```

## 📊 Мониторинг

Настройте мониторинг сервисов:

```bash
# PM2 мониторинг
pm2 monitor

# Или используйте pm2-logrotate
pm2 install pm2-logrotate
```

## 🔄 Автоматическое обновление

Настройте автоматическое обновление SSL:

```bash
# Проверка автообновления
sudo certbot renew --dry-run

# Добавьте в cron (уже настроено автоматически при установке)
sudo crontab -l
```

## 📝 Рекомендации

1. **Регулярные бэкапы**: Создавайте резервные копии data/elclasico.json
2. **Обновления**: Регулярно обновляйте зависимости: `npm update`
3. **Логи**: Периодически проверяйте логи: `pm2 logs`
4. **Мониторинг**: Следите за использованием ресурсов: `pm2 monit`

## 🚨 Что делать при проблемах

### Сайт не открывается:
```bash
pm2 status
sudo systemctl status nginx
sudo netstat -tulpn | grep :3000
```

### Ошибки 502 Bad Gateway:
```bash
pm2 restart elclasico
sudo nginx -t
sudo systemctl reload nginx
```

### База данных не обновляется:
```bash
# Проверьте права доступа
ls -la data/
chmod 644 data/elclasico.json
```

## 📞 Контакты поддержки

При критических проблемах:
1. Проверьте RELEASE_SUMMARY.md
2. Посмотрите QUICKSTART.md
3. Изучите логи: `pm2 logs elclasico`

---

**Помните**: Всегда делайте резервные копии перед обновлениями! 🔒
