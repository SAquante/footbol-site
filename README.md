# 🏆 Amateur El Clásico

<div align="center">

**Официальный веб-сайт любительских футбольных команд "Реал Мадрид" и "Барселона"**

Современный, быстрый и визуально уникальный веб-сайт для управления расписанием матчей, составами и результатами игр.

</div>

---

## ✨ Особенности

### 🎨 Суперсовременный дизайн
- **Темная тема** по умолчанию с неоновыми акцентами
- **Асимметричная верстка** в духе противостояния Эль Класико
- **Плавные анимации** и микро-взаимодействия (Framer Motion)
- **Responsive дизайн** - идеально работает на всех устройствах
- **Градиенты команд**: золото/белый для Реала, синий/красный для Барселоны

### ⚡ Функциональность

#### Для игроков:
- 📅 Просмотр расписания предстоящих и прошедших матчей
- ⏱️ Таймер обратного отсчета до следующего матча
- 👥 Просмотр составов на игру
- 📊 Результаты завершенных матчей

#### Для администратора:
- ➕ Создание новых матчей
- ✏️ Редактирование информации о матчах
- 📝 Внесение составов команд
- ⚽ Добавление итоговых счетов
- 🗑️ Удаление матчей

### 🔒 Безопасность
- JWT аутентификация
- Защищенная админ-панель
- Система ролей (ADMIN / PLAYER)

---

## 🛠️ Технологический стек

- **Frontend**: Next.js 14 (React 18), TypeScript
- **Styling**: Tailwind CSS
- **Анимации**: Framer Motion
- **Backend**: Next.js API Routes
- **База данных**: PostgreSQL
- **Аутентификация**: JWT + bcryptjs
- **Даты**: date-fns
- **State Management**: Zustand

---

## 📋 Предварительные требования

Перед установкой убедитесь, что у вас установлены:

- **Node.js** версии 18.x или выше
- **PostgreSQL** версии 14 или выше
- **npm** или **yarn**

---

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
cd "footbol site"
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка базы данных PostgreSQL

#### Создание базы данных:

```bash
# Войдите в PostgreSQL
psql -U postgres

# Создайте базу данных
CREATE DATABASE elclasico;

# Выйдите
\q
```

### 4. Настройка переменных окружения

Файл `.env.local` уже создан. Отредактируйте его:

```env
# Обновите эти значения под вашу установку PostgreSQL
DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/elclasico

# Сгенерируйте сильные секретные ключи для production
JWT_SECRET=ваш-супер-секретный-jwt-ключ-измените-в-продакшн
NEXTAUTH_SECRET=ваш-nextauth-секретный-ключ
```

### 5. Инициализация базы данных

```bash
npm run db:init
```

Эта команда:
- Создаст все необходимые таблицы
- Настроит индексы
- Создаст администратора по умолчанию:
  - **Логин**: `admin`
  - **Пароль**: `admin123`

> ⚠️ **Важно**: Смените пароль администратора сразу после первого входа!

### 6. Запуск приложения

#### Режим разработки:

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

#### Production сборка:

```bash
# Создать оптимизированную сборку
npm run build

# Запустить production сервер
npm start
```

---

## 👤 Учетные данные по умолчанию

### Администратор:
- **Логин**: `admin`
- **Пароль**: `admin123`

### Создание новых пользователей:
Новые игроки могут зарегистрироваться через страницу регистрации. Они получат роль `PLAYER` автоматически.

---

## 📁 Структура проекта

```
footbol site/
├── app/                      # Next.js App Router
│   ├── admin/               # Админ-панель
│   │   ├── create/         # Создание матча
│   │   ├── edit/[id]/      # Редактирование матча
│   │   └── page.tsx        # Главная админки
│   ├── api/                # API Routes
│   │   ├── auth/           # Аутентификация
│   │   └── matches/        # CRUD матчей
│   ├── login/              # Страница входа
│   ├── register/           # Страница регистрации
│   ├── schedule/           # Расписание
│   ├── match/[id]/         # Детали матча
│   ├── globals.css         # Глобальные стили
│   ├── layout.tsx          # Root layout
│   └── page.tsx            # Главная страница
├── components/             # React компоненты
│   ├── CountdownTimer.tsx  # Таймер обратного отсчета
│   ├── MatchCard.tsx       # Карточка матча
│   └── Navigation.tsx      # Навигация
├── database/              # SQL схемы
│   └── schema.sql         # Схема БД
├── lib/                   # Утилиты
│   ├── db.ts             # Подключение к БД
│   └── auth.ts           # JWT утилиты
├── scripts/              # Скрипты
│   └── init-db.js        # Инициализация БД
├── store/                # State management
│   └── authStore.ts      # Zustand store
├── types/                # TypeScript типы
│   └── index.ts
└── package.json
```

---

## 🎯 Использование

### Для игроков:

1. **Зарегистрируйтесь** или **войдите** в систему
2. На **главной странице** увидите следующий матч с таймером и составами
3. В **расписании** - все предстоящие и прошедшие матчи
4. Кликните на матч для просмотра деталей

### Для администратора:

1. **Войдите** с учеткой администратора
2. Перейдите в **Админ-панель**
3. **Создайте новый матч**: укажите дату, время и место
4. **Редактируйте матч**: добавьте составы, измените статус на "Сыгран", внесите счет
5. **Удаляйте** отмененные матчи

---

## 🎨 Кастомизация

### Изменение цветов команд

Отредактируйте `tailwind.config.js`:

```js
colors: {
  'real-gold': '#FFD700',      // Цвет Реала
  'real-white': '#FFFFFF',
  'barca-blue': '#004D98',     // Цвет Барселоны
  'barca-red': '#DC0028',
  'neon-blue': '#00F0FF',      // Неоновые акценты
  'neon-red': '#FF003C',
}
```

### Изменение шрифта

В `app/globals.css` замените импорт Google Fonts:

```css
@import url('https://fonts.googleapis.com/css2?family=ВАШ_ШРИФТ&display=swap');
```

---

## 🔧 API Endpoints

### Аутентификация

```
POST /api/auth/login       - Вход в систему
POST /api/auth/register    - Регистрация
```

### Матчи

```
GET    /api/matches        - Получить все матчи
POST   /api/matches        - Создать матч (требует ADMIN)
GET    /api/matches/[id]   - Получить матч по ID
PUT    /api/matches/[id]   - Обновить матч (требует ADMIN)
DELETE /api/matches/[id]   - Удалить матч (требует ADMIN)
```

---

## 🐛 Устранение неполадок

### Ошибка подключения к БД

```
Error: connect ECONNREFUSED 127.0.0.1:5432
```

**Решение**: Убедитесь, что PostgreSQL запущен:

```bash
# Windows (PowerShell от администратора)
net start postgresql-x64-14

# Или проверьте в Services
```

### Ошибка "MODULE_NOT_FOUND"

```bash
# Переустановите зависимости
Remove-Item -Recurse -Force node_modules
npm install
```

### Ошибки TypeScript при разработке

TypeScript ошибки в VSCode исчезнут после установки зависимостей. Перезагрузите VSCode если они остались.

---

## 📝 Схема базы данных

### Таблица `users`

| Поле          | Тип        | Описание              |
|---------------|------------|-----------------------|
| id            | SERIAL     | Primary Key           |
| username      | VARCHAR    | Логин (уникальный)    |
| password_hash | VARCHAR    | Хэш пароля (bcrypt)   |
| role          | VARCHAR    | ADMIN или PLAYER      |
| created_at    | TIMESTAMP  | Дата создания         |

### Таблица `matches`

| Поле           | Тип        | Описание                    |
|----------------|------------|-----------------------------|
| id             | SERIAL     | Primary Key                 |
| match_datetime | TIMESTAMP  | Дата и время матча          |
| location       | VARCHAR    | Место проведения            |
| status         | VARCHAR    | scheduled / completed       |
| score_real     | INTEGER    | Счет Реала (nullable)       |
| score_barca    | INTEGER    | Счет Барселоны (nullable)   |
| lineup_real    | TEXT       | Состав Реала (nullable)     |
| lineup_barca   | TEXT       | Состав Барселоны (nullable) |
| created_at     | TIMESTAMP  | Дата создания               |
| updated_at     | TIMESTAMP  | Дата обновления             |

---

## 🚀 Деплой

### Vercel (рекомендуется для Next.js)

1. Создайте аккаунт на [Vercel](https://vercel.com)
2. Установите Vercel CLI:

```bash
npm i -g vercel
```

3. Деплой:

```bash
vercel
```

4. Настройте переменные окружения в Vercel Dashboard
5. Используйте управляемую PostgreSQL БД (Vercel Postgres, Supabase, или Railway)

### Другие платформы

Проект можно задеплоить на:
- Railway
- Render
- DigitalOcean App Platform
- AWS / Azure / GCP

---

## 📄 Лицензия

Этот проект создан для личного использования любительскими футбольными командами.

---

## 🤝 Поддержка

Если возникли вопросы или проблемы:

1. Проверьте раздел **Устранение неполадок**
2. Убедитесь, что PostgreSQL запущен и настроен
3. Проверьте `.env.local` файл
4. Пересоздайте базу данных: `npm run db:init`

---

<div align="center">

**Сделано с ❤️ для любительских команд**

⚽ **Hala Madrid!** | **Visca Barça!** ⚽

</div>
