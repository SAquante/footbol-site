# 🤝 Как внести вклад

Спасибо за интерес к проекту Amateur El Clásico! Мы рады любому вкладу.

## 🐛 Нашли баг?

1. Проверьте, что баг еще не [зарепорчен](https://github.com/YOUR_USERNAME/amateur-el-clasico/issues)
2. Если нет, создайте новый Issue с меткой `bug`
3. Опишите:
   - Что вы делали
   - Что ожидали увидеть
   - Что увидели на самом деле
   - Шаги для воспроизведения
   - Скриншоты (если возможно)

## 💡 Есть идея?

1. Создайте Issue с меткой `enhancement`
2. Опишите вашу идею подробно
3. Объясните, зачем это нужно
4. Дождитесь обсуждения перед началом работы

## 🔨 Хотите добавить функцию?

### Шаг 1: Fork репозитория

Нажмите кнопку "Fork" на GitHub

### Шаг 2: Клонируйте свой fork

```bash
git clone https://github.com/YOUR_USERNAME/amateur-el-clasico.git
cd amateur-el-clasico
```

### Шаг 3: Создайте ветку

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/your-bug-fix
```

### Шаг 4: Установите зависимости

```bash
npm install
```

### Шаг 5: Запустите dev сервер

```bash
npm run dev
```

### Шаг 6: Внесите изменения

- Следуйте существующему стилю кода
- Используйте TypeScript
- Добавьте комментарии к сложному коду
- Проверьте на мобильных устройствах

### Шаг 7: Проверьте работу

```bash
# Проверьте линтинг
npm run lint

# Сделайте билд
npm run build

# Запустите production версию
npm start
```

### Шаг 8: Закоммитьте изменения

```bash
git add .
git commit -m "✨ Add: описание вашей фичи"
```

Используйте эмодзи в коммитах:
- ✨ `:sparkles:` - новая фича
- 🐛 `:bug:` - исправление бага
- 📝 `:memo:` - документация
- 💄 `:lipstick:` - UI/стили
- ♻️ `:recycle:` - рефакторинг
- ⚡ `:zap:` - улучшение производительности
- 🔒 `:lock:` - безопасность

### Шаг 9: Отправьте в свой fork

```bash
git push origin feature/your-feature-name
```

### Шаг 10: Создайте Pull Request

1. Перейдите на GitHub в свой fork
2. Нажмите "Compare & pull request"
3. Заполните описание PR:
   - Что изменили
   - Зачем это нужно
   - Ссылка на Issue (если есть)
   - Скриншоты (если UI изменения)
4. Дождитесь ревью

## 📋 Стандарты кода

### TypeScript

```typescript
// ✅ Хорошо
interface Match {
  id: number;
  status: 'scheduled' | 'completed';
  score_real: number | null;
}

// ❌ Плохо
interface Match {
  id: any;
  status: string;
  score_real: number;
}
```

### React компоненты

```tsx
// ✅ Хорошо
export default function MatchCard({ match }: { match: Match }) {
  return (
    <div className="card">
      {/* ... */}
    </div>
  );
}

// ❌ Плохо
export default function MatchCard(props: any) {
  return <div>{/* ... */}</div>;
}
```

### API Routes

```typescript
// ✅ Хорошо
export async function GET(request: NextRequest) {
  try {
    const data = await getMatches();
    return NextResponse.json(data);
  } catch (error) {
    return NextResponse.json(
      { error: 'Ошибка получения данных' },
      { status: 500 }
    );
  }
}

// ❌ Плохо
export async function GET(request: any) {
  const data = getMatches();
  return NextResponse.json(data);
}
```

## 🎨 Дизайн гайдлайны

- Используйте темную тему по умолчанию
- Градиенты команд: золото/белый (Реал), синий/красный (Барса)
- Минимальный размер кнопок: 44x44px (для touch)
- Используйте Framer Motion для анимаций
- Все должно работать на мобильных

## 📱 Тестирование на мобильных

Проверьте на разных разрешениях:
- 375x812 (iPhone X)
- 414x896 (iPhone XR)
- 360x640 (Android)
- 768x1024 (iPad)
- 1440x900 (Desktop)

## 🚫 Что НЕ принимается

- ❌ Код без TypeScript типов
- ❌ Изменения без описания
- ❌ Нарушение существующего стиля
- ❌ Код без проверки на mobile
- ❌ Хардкод паролей/токенов
- ❌ Удаление важного функционала

## ✅ Checklist перед PR

- [ ] Код работает локально
- [ ] Нет TypeScript ошибок
- [ ] Build проходит успешно
- [ ] Проверено на мобильных
- [ ] Добавлены комментарии
- [ ] Обновлена документация (если нужно)
- [ ] Коммит сообщения понятные

## 🏆 Наши контрибьюторы

Спасибо всем, кто вносит вклад в проект! ⚽

---

## 📞 Вопросы?

Если что-то непонятно:
- Создайте [Discussion](https://github.com/YOUR_USERNAME/amateur-el-clasico/discussions)
- Напишите на email: dev@yesfootboll.online
- Telegram: @elclasico_dev

---

**Спасибо за ваш вклад! 🙏**
