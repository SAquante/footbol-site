server {
    listen 80;
    server_name ваш-домен.ru www.ваш-домен.ru;

    # Увеличиваем размер загружаемых файлов
    client_max_body_size 50M;

    # Основное приложение Next.js
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Таймауты
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Статические файлы (если нужно кэширование)
    location /_next/static/ {
        proxy_pass http://localhost:3000;
        proxy_cache_valid 200 60m;
        add_header Cache-Control "public, immutable";
    }

    # Изображения
    location ~* \.(jpg|jpeg|png|gif|ico|svg|webp)$ {
        proxy_pass http://localhost:3000;
        proxy_cache_valid 200 7d;
        add_header Cache-Control "public, max-age=604800";
    }

    # Логи
    access_log /var/log/nginx/footbol-site_access.log;
    error_log /var/log/nginx/footbol-site_error.log;
}

# HTTPS редирект (раскомментируйте после установки SSL)
# server {
#     listen 80;
#     server_name ваш-домен.ru www.ваш-домен.ru;
#     return 301 https://$server_name$request_uri;
# }

# SSL конфигурация (будет автоматически добавлена certbot)
