# 🚀 БЫСТРЫЙ СТАРТ: Развертывание на Reg.ru

## 📋 Что уже готово:

✅ Все конфигурационные файлы созданы  
✅ Скрипты развертывания подготовлены  
✅ Проект готов к загрузке на сервер

---

## 🎯 3 ПРОСТЫХ ШАГА

### 1️⃣ Закажите VPS на Reg.ru
- Зайдите на https://www.reg.ru/vps/
- Выберите тариф (минимум: 1GB RAM, 20GB SSD)
- ОС: **Ubuntu 22.04**
- Получите IP адрес и пароль root

### 2️⃣ Загрузите файлы на сервер

**ВАРИАНТ А (Проще):** FileZilla
1. Скачайте FileZilla: https://filezilla-project.org/
2. Подключитесь: `sftp://ваш_IP`, логин: `root`, порт: `22`
3. Перетащите ВСЕ файлы в `/var/www/footbol-site/`
   (Кроме `node_modules` и `.next`)

**ВАРИАНТ Б:** GitHub
```bash
# На вашем компьютере:
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/footbol-site.git
git push -u origin main
```

### 3️⃣ Настройте сервер

Подключитесь по SSH (через PuTTY или терминал):
```bash
ssh root@ваш_IP
```

Выполните команды:
```bash
# 1. Дайте права на выполнение скрипта
chmod +x /var/www/footbol-site/deploy.sh

# 2. Запустите автоматическую установку
/var/www/footbol-site/deploy.sh

# 3. Установите зависимости
cd /var/www/footbol-site
npm install

# 4. Соберите проект
npm run build

# 5. Настройте Nginx
cp nginx.conf /etc/nginx/sites-available/footbol-site
nano /etc/nginx/sites-available/footbol-site
# Замените 'ваш-домен.ru' на ваш реальный домен
# Сохраните: Ctrl+X, Y, Enter

ln -s /etc/nginx/sites-available/footbol-site /etc/nginx/sites-enabled/
nginx -t
systemctl reload nginx

# 6. Запустите приложение
pm2 start ecosystem.config.json
pm2 startup
pm2 save

# 7. Настройте SSL
certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru
```

---

## 🌐 Настройка домена на Reg.ru

1. Панель управления Reg.ru → "DNS-серверы и зона"
2. Добавьте A-записи:
   ```
   @    ->  A  ->  IP_вашего_VPS
   www  ->  A  ->  IP_вашего_VPS
   ```
3. Дождитесь обновления DNS (1-24 часа)

---

## ✅ Готово!

Ваш сайт доступен по адресу: **https://ваш-домен.ru**

---

## 📚 Подробные инструкции

- **DEPLOYMENT.md** - Полная инструкция по развертыванию
- **WINDOWS_GUIDE.md** - Инструкция для Windows пользователей
- **update.sh** - Скрипт быстрого обновления сайта

---

## 🔧 Полезные команды

```bash
# Просмотр логов
pm2 logs footbol-site

# Перезапуск
pm2 restart footbol-site

# Обновление сайта (после изменений)
cd /var/www/footbol-site
chmod +x update.sh
./update.sh
```

---

## 🆘 Проблемы?

1. **Сайт не открывается**: Проверьте `pm2 status` и `systemctl status nginx`
2. **502 Bad Gateway**: Перезапустите `pm2 restart footbol-site`
3. **DNS не обновился**: Подождите до 24 часов

Логи:
```bash
pm2 logs footbol-site --lines 100
tail -f /var/log/nginx/footbol-site_error.log
```

---

## 💰 Стоимость

- VPS Reg.ru: от 199₽/месяц
- Домен .ru: от 169₽/год
- SSL сертификат: **БЕСПЛАТНО** (Let's Encrypt)

**Итого:** ~400₽/месяц за полноценный сайт с HTTPS

---

## 📞 Поддержка

- Reg.ru: support@reg.ru, +7 (495) 580-11-11
- Документация: https://help.reg.ru/support/vps
