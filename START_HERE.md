# 🚀 ИНСТРУКЦИЯ ПО ЗАПУСКУ

## Что уже сделано ✅

Проект **"Amateur El Clásico"** полностью создан и готов к запуску!

### Создано:

- ✅ **Next.js 14** приложение с TypeScript
- ✅ **PostgreSQL** схема базы данных
- ✅ **API Routes** для матчей и аутентификации
- ✅ **Суперсовременный UI** с темной темой и анимациями
- ✅ **Админ-панель** для управления матчами
- ✅ **Система аутентификации** с JWT
- ✅ **Responsive дизайн** для мобильных устройств

---

## 📝 ЧТО ДЕЛАТЬ СЕЙЧАС

### 1️⃣ Установите зависимости

Откройте терминал PowerShell в папке проекта и выполните:

```powershell
npm install
```

⏱️ Это займет 2-3 минуты.

### 2️⃣ Установите PostgreSQL

**Если еще не установлен:**

1. Скачайте: https://www.postgresql.org/download/windows/
2. Запустите установщик
3. Запомните пароль для пользователя `postgres`
4. Используйте порт `5432` (по умолчанию)

### 3️⃣ Создайте базу данных

```powershell
# Подключитесь к PostgreSQL
psql -U postgres

# Создайте базу данных (в консоли PostgreSQL)
CREATE DATABASE elclasico;

# Выйдите
\q
```

### 4️⃣ Настройте подключение к БД

Откройте файл `.env.local` и измените строку:

```env
DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/elclasico
```

Замените `ВАШ_ПАРОЛЬ` на ваш пароль PostgreSQL.

### 5️⃣ Инициализируйте базу данных

```powershell
npm run db:init
```

Вы должны увидеть:
```
✅ База данных успешно инициализирована!
👤 Создан администратор: username = "admin", password = "admin123"
```

### 6️⃣ Запустите проект!

```powershell
npm run dev
```

Откройте браузер: **http://localhost:3000**

---

## 🎯 Первые шаги после запуска

### 1. Войдите как администратор

- Нажмите **"Вход"** в правом верхнем углу
- Логин: `admin`
- Пароль: `admin123`

### 2. Создайте первый матч

- Нажмите **"Админ-панель"**
- Кликните **"Создать матч"**
- Заполните:
  - Дату
  - Время (например, 20:00)
  - Место проведения (например, "Стадион Луч")
- Нажмите **"Создать матч"**

### 3. Добавьте составы команд

- В админ-панели найдите созданный матч
- Нажмите **"Редактировать"**
- Прокрутите до секции **"Составы команд"**
- Введите имена игроков (каждое имя с новой строки):
  
  **Реал Мадрид:**
  ```
  Вася
  Петя
  Коля
  Саша
  Дима
  ```
  
  **Барселона:**
  ```
  Миша
  Алекс
  Виктор
  Сергей
  Андрей
  ```

- Нажмите **"Сохранить изменения"**

### 4. Посмотрите результат

- Перейдите на **главную страницу**
- Увидите таймер обратного отсчета
- Составы команд
- Место и время матча

### 5. После игры внесите счет

- Вернитесь в админ-панель
- Нажмите **"Редактировать"** на матче
- Измените **Статус** на "Сыгран"
- Введите **Итоговый счет** (например, 3:2)
- Сохраните

---

## 🎨 Особенности дизайна

### Суперсовременный UI:
- 🌑 **Темная тема** с неоновыми акцентами
- ✨ **Плавные анимации** при прокрутке
- ⚡ **Градиенты команд**: золото для Реала, сине-красный для Барсы
- 📱 **Полностью адаптивный** - работает на телефонах
- 🎯 **Эффект VS** на странице матча

### Функциональность:
- ⏱️ **Таймер обратного отсчета** до следующего матча
- 📅 **Расписание** всех матчей
- 👥 **Составы команд** для каждого матча
- ⚽ **Результаты** завершенных игр
- 🔒 **Защищенная админ-панель**

---

## ⚠️ Если что-то не работает

### Ошибка подключения к БД:

```
Error: connect ECONNREFUSED
```

**Решение:**
1. Убедитесь, что PostgreSQL запущен:
   ```powershell
   net start postgresql-x64-14
   ```
2. Проверьте пароль в `.env.local`

### Ошибка "MODULE_NOT_FOUND":

**Решение:**
```powershell
Remove-Item -Recurse -Force node_modules
npm install
```

### PostgreSQL не найден:

**Решение:**
1. Откройте "Службы" (Services)
2. Найдите PostgreSQL
3. Запустите службу вручную

---

## 📱 Использование на телефоне

### В одной Wi-Fi сети:

1. Узнайте IP компьютера:
   ```powershell
   ipconfig
   ```
   Ищите "IPv4 Address" (например, `192.168.1.100`)

2. На телефоне откройте:
   ```
   http://192.168.1.100:3000
   ```

---

## 📚 Дополнительная документация

- **README.md** - полная документация проекта
- **QUICKSTART.md** - быстрый старт и FAQ
- **API_EXAMPLES.md** - примеры работы с API

---

## 🎮 Команды для разработки

```powershell
# Запуск в режиме разработки
npm run dev

# Создание production сборки
npm run build

# Запуск production версии
npm start

# Инициализация/сброс базы данных
npm run db:init
```

---

## 🔐 Учетные данные

### Администратор по умолчанию:
- **Логин:** `admin`
- **Пароль:** `admin123`

⚠️ **Важно:** Смените пароль после первого входа!

### Регистрация игроков:
Новые пользователи могут зарегистрироваться на странице "Регистрация" и получат роль PLAYER.

---

## 🌐 Структура проекта

```
footbol site/
├── app/              # Страницы Next.js
│   ├── admin/       # Админ-панель
│   ├── api/         # API маршруты
│   ├── login/       # Страница входа
│   ├── register/    # Регистрация
│   └── ...
├── components/      # React компоненты
├── database/        # SQL схемы
├── lib/            # Утилиты
└── store/          # State management
```

---

## 🎉 Готово!

Проект полностью настроен и готов к использованию!

### Следующие шаги:

1. ✅ Установите зависимости: `npm install`
2. ✅ Настройте PostgreSQL и `.env.local`
3. ✅ Инициализируйте БД: `npm run db:init`
4. ✅ Запустите: `npm run dev`
5. ✅ Создайте первый матч в админ-панели

---

<div align="center">

**Удачи с вашими матчами!** ⚽

Hala Madrid! | Visca Barça!

</div>
