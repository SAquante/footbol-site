# 🔒 Безопасность

## Важно перед деплоем!

### 1. Смените пароли администратора

⚠️ **КРИТИЧЕСКИ ВАЖНО**: Тестовый пароль суперадминистратора `qopdota2` известен всем!

Измените пароль в файле `data/elclasico.json`:

```bash
# Сгенерируйте новый хеш пароля
node -e "const bcrypt = require('bcryptjs'); console.log(bcrypt.hashSync('ВАШ_НОВЫЙ_ПАРОЛЬ', 10));"
```

Затем замените `password_hash` для пользователя Сергей в `data/elclasico.json`.

### 2. Переменные окружения

Создайте файл `.env.local` (не коммитьте его в Git!):

```env
# JWT секретный ключ - ОБЯЗАТЕЛЬНО измените!
JWT_SECRET=ваш-очень-длинный-и-случайный-секретный-ключ-минимум-32-символа

# URL продакшен сервера
NEXT_PUBLIC_API_URL=https://yesfootboll.online
```

### 3. Защита базы данных

Файл `data/elclasico.json` содержит:
- ✅ Хешированные пароли (безопасно)
- ⚠️ Email адреса (публичная информация)
- ⚠️ История матчей (публичная информация)

Если нужна полная конфиденциальность данных, добавьте в `.gitignore`:
```
data/elclasico.json
```

### 4. CORS и API защита

API роуты защищены JWT токенами:
- `/api/auth/*` - публичные (login, register)
- `/api/matches` GET - публичный (чтение)
- `/api/matches` POST/PUT/DELETE - требует JWT (superadmin/admin)
- `/api/users/*` - требует JWT (superadmin)

### 5. Nginx конфигурация

Убедитесь, что в `nginx.conf` настроены:
- SSL сертификаты (Let's Encrypt)
- Защита от DDoS
- Rate limiting
- Заголовки безопасности

### 6. PM2 мониторинг

```bash
# Просмотр логов на ошибки безопасности
pm2 logs amateur-el-clasico --err

# Мониторинг использования ресурсов
pm2 monit
```

### 7. Регулярные обновления

```bash
# Проверка уязвимостей в зависимостях
npm audit

# Автоматическое исправление
npm audit fix

# Обновление зависимостей
npm update
```

## Сообщить о уязвимости

Если вы нашли уязвимость в безопасности, пожалуйста:
1. **НЕ** создавайте публичный Issue
2. Напишите на email: security@yesfootboll.online
3. Опишите проблему и шаги для воспроизведения

Мы ответим в течение 48 часов.

---

© 2025 Amateur El Clásico Team
