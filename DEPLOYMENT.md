# 🚀 Инструкция по развертыванию на Reg.ru VPS

## Предварительные требования

- VPS/VDS на Reg.ru (минимум 1GB RAM, Ubuntu 22.04)
- Доменное имя, привязанное к IP вашего VPS
- SSH доступ к серверу

---

## 📋 Пошаговая инструкция

### Шаг 1: Подключение к VPS

```bash
ssh root@ваш_ip_адрес
```

Введите пароль, который вы получили от Reg.ru

---

### Шаг 2: Загрузка файла deploy.sh

```bash
# Загрузите deploy.sh на сервер через FTP или создайте вручную:
nano deploy.sh
# Вставьте содержимое файла deploy.sh
# Нажмите Ctrl+X, затем Y, затем Enter

# Дайте права на выполнение
chmod +x deploy.sh

# Запустите скрипт
./deploy.sh
```

Скрипт автоматически установит:
- Node.js 20.x
- PM2 (менеджер процессов)
- Nginx (веб-сервер)
- Git

---

### Шаг 3: Загрузка проекта на сервер

#### Вариант А: Через FTP/SFTP (FileZilla)

1. Подключитесь к серверу через SFTP:
   - Host: `sftp://ваш_ip_адрес`
   - Username: `root`
   - Password: ваш пароль
   - Port: `22`

2. Загрузите все файлы проекта в `/var/www/footbol-site/`

#### Вариант Б: Через GitHub (рекомендуется)

```bash
cd /var/www/footbol-site
git clone https://github.com/ваш-username/footbol-site.git .
```

---

### Шаг 4: Установка и сборка проекта

```bash
cd /var/www/footbol-site

# Установка зависимостей
npm install

# Создание production build
npm run build
```

---

### Шаг 5: Настройка Nginx

```bash
# Скопируйте конфиг Nginx
cp nginx.conf /etc/nginx/sites-available/footbol-site

# Откройте файл и замените 'ваш-домен.ru' на ваш реальный домен
nano /etc/nginx/sites-available/footbol-site

# Активируйте конфиг
ln -s /etc/nginx/sites-available/footbol-site /etc/nginx/sites-enabled/

# Проверьте конфигурацию
nginx -t

# Если всё ОК, перезапустите Nginx
systemctl reload nginx
```

---

### Шаг 6: Запуск приложения через PM2

```bash
# Запустите приложение
pm2 start ecosystem.config.json

# Настройте автозапуск при перезагрузке сервера
pm2 startup
pm2 save

# Проверьте статус
pm2 status
pm2 logs footbol-site
```

---

### Шаг 7: Настройка DNS на Reg.ru

1. Войдите в панель управления Reg.ru
2. Перейдите в раздел "DNS-серверы и зона"
3. Добавьте A-записи:
   ```
   @ (или пусто)  ->  A  ->  IP_вашего_VPS
   www            ->  A  ->  IP_вашего_VPS
   ```
4. Дождитесь обновления DNS (до 24 часов, обычно 1-2 часа)

---

### Шаг 8: Установка SSL сертификата (HTTPS)

```bash
# Установите Certbot
apt install -y certbot python3-certbot-nginx

# Получите бесплатный SSL сертификат от Let's Encrypt
certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru

# Следуйте инструкциям на экране
# Введите email
# Согласитесь с условиями
# Выберите опцию редиректа HTTP -> HTTPS
```

Сертификат будет автоматически обновляться каждые 90 дней.

---

## 🔧 Полезные команды PM2

```bash
# Просмотр логов
pm2 logs footbol-site

# Перезапуск приложения
pm2 restart footbol-site

# Остановка приложения
pm2 stop footbol-site

# Удаление из PM2
pm2 delete footbol-site

# Обновление после изменений
cd /var/www/footbol-site
git pull                    # Если используете Git
npm install                 # Обновление зависимостей
npm run build              # Пересборка
pm2 restart footbol-site   # Перезапуск
```

---

## 🛡️ Настройки безопасности (рекомендуется)

### 1. Настройка Firewall

```bash
# Установка UFW
apt install -y ufw

# Разрешите SSH, HTTP, HTTPS
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp

# Включите firewall
ufw enable

# Проверьте статус
ufw status
```

### 2. Создание непривилегированного пользователя

```bash
# Создайте пользователя
adduser footbol

# Добавьте в группу sudo
usermod -aG sudo footbol

# Переключитесь на пользователя
su - footbol

# Теперь используйте этого пользователя вместо root
```

### 3. Настройка автоматических обновлений

```bash
apt install -y unattended-upgrades
dpkg-reconfigure -plow unattended-upgrades
```

---

## 📊 Мониторинг

### Просмотр использования ресурсов

```bash
# CPU и память
htop

# Дисковое пространство
df -h

# Сетевые подключения
netstat -tulpn | grep :3000
netstat -tulpn | grep :80
```

### Логи Nginx

```bash
# Access log
tail -f /var/log/nginx/footbol-site_access.log

# Error log
tail -f /var/log/nginx/footbol-site_error.log
```

---

## 🔄 Обновление сайта

```bash
# 1. Загрузите новые файлы через FTP или Git
cd /var/www/footbol-site
git pull  # Если используете Git

# 2. Установите новые зависимости (если есть)
npm install

# 3. Пересоберите проект
npm run build

# 4. Перезапустите приложение
pm2 restart footbol-site
```

---

## 🆘 Устранение неполадок

### Проблема: Сайт не открывается

```bash
# Проверьте статус Nginx
systemctl status nginx

# Проверьте статус приложения
pm2 status

# Проверьте логи
pm2 logs footbol-site --lines 100
```

### Проблема: "502 Bad Gateway"

```bash
# Проверьте, запущено ли приложение
pm2 status

# Перезапустите приложение
pm2 restart footbol-site

# Проверьте логи на ошибки
pm2 logs footbol-site --err
```

### Проблема: Недостаточно памяти

```bash
# Увеличьте лимит памяти в ecosystem.config.json
nano ecosystem.config.json
# Измените "max_memory_restart": "2G"

# Перезапустите
pm2 reload ecosystem.config.json
```

---

## 📞 Контакты поддержки

- Reg.ru: support@reg.ru, +7 (495) 580-11-11
- Документация Reg.ru VPS: https://help.reg.ru/support/vps

---

## ✅ Чек-лист развертывания

- [ ] VPS заказан и настроен
- [ ] SSH доступ работает
- [ ] Скрипт deploy.sh выполнен
- [ ] Файлы проекта загружены на сервер
- [ ] `npm install` и `npm run build` выполнены
- [ ] Nginx настроен и перезапущен
- [ ] PM2 запустил приложение
- [ ] DNS настроен на Reg.ru
- [ ] SSL сертификат установлен
- [ ] Сайт открывается по HTTPS
- [ ] Firewall настроен
- [ ] Логи проверены на ошибки

---

## 🎉 Готово!

Ваш сайт должен быть доступен по адресу: **https://ваш-домен.ru**

Если возникли проблемы, проверьте логи:
```bash
pm2 logs footbol-site
tail -f /var/log/nginx/footbol-site_error.log
```
