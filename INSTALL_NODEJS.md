# ⚠️ ВАЖНО: Node.js не установлен!

## 🔴 Обнаружена проблема

На вашем компьютере **не установлен Node.js**, который необходим для работы проекта.

---

## ✅ Решение - Установка Node.js

### Шаг 1: Скачайте Node.js

1. Откройте браузер
2. Перейдите на: **https://nodejs.org/**
3. Скачайте **LTS версию** (Long Term Support) - обычно это кнопка слева
   - На момент написания это версия 20.x или 18.x
4. Выберите установщик для Windows (`.msi` файл)

### Шаг 2: Установите Node.js

1. Запустите скачанный файл (например, `node-v20.x.x-x64.msi`)
2. Следуйте инструкциям установщика:
   - ✅ Согласитесь с лицензией
   - ✅ Оставьте стандартный путь установки
   - ✅ **ВАЖНО**: Убедитесь, что отмечена галочка **"Add to PATH"**
   - ✅ Можно установить дополнительные инструменты (Chocolatey) - необязательно
3. Нажмите "Install"
4. Дождитесь завершения установки

### Шаг 3: Проверьте установку

1. **Перезапустите PowerShell** (закройте и откройте заново)
2. Выполните команду:

```powershell
node --version
```

Вы должны увидеть что-то вроде: `v20.10.0`

3. Проверьте npm:

```powershell
npm --version
```

Должно показать версию, например: `10.2.3`

---

## 🚀 После установки Node.js

### 1. Перейдите в папку проекта

```powershell
cd "C:\Users\я\Desktop\footbol site"
```

### 2. Установите зависимости

```powershell
npm install
```

⏱️ Это займет 2-3 минуты. Будут установлены все необходимые пакеты.

### 3. Инициализируйте базу данных

```powershell
npm run db:init
```

Вы увидите:
```
✅ База данных успешно инициализирована!
👤 Создан администратор: username = "admin", password = "admin123"
```

### 4. Запустите проект

```powershell
npm run dev
```

### 5. Откройте браузер

Перейдите на: **http://localhost:3000**

---

## 📝 Изменения в проекте

### ✅ База данных заменена на SQLite

**Было:** PostgreSQL (требует установки отдельного сервера)

**Стало:** SQLite (легковесная, встроенная БД)

### Преимущества SQLite:

- ✅ **Не требует установки** отдельного сервера БД
- ✅ **Один файл** - вся БД в файле `data/elclasico.db`
- ✅ **Портативность** - легко бэкапить и переносить
- ✅ **Быстрый старт** - работает сразу после `npm run db:init`
- ✅ **Идеально для небольших проектов**

### Что изменилось:

1. **package.json** - заменил `pg` на `better-sqlite3`
2. **lib/db.ts** - адаптирован под SQLite
3. **database/schema.sql** - синтаксис SQLite
4. **scripts/init-db.js** - работает с SQLite
5. **.env.local** - теперь путь к файлу БД вместо URL

### База данных будет храниться в:
```
footbol site/
└── data/
    └── elclasico.db  <- Здесь вся БД
```

---

## 🎯 Краткая инструкция

После установки Node.js выполните по порядку:

```powershell
# 1. Установите зависимости
npm install

# 2. Создайте БД
npm run db:init

# 3. Запустите проект
npm run dev
```

Готово! Откройте **http://localhost:3000**

---

## ❓ Если что-то не работает

### "npm" не распознан (после установки Node.js)

**Решение:** Перезапустите PowerShell или перезагрузите компьютер

### Ошибки при npm install

**Решение:** 
```powershell
# Очистите кэш npm
npm cache clean --force

# Попробуйте снова
npm install
```

### Ошибка при npm run db:init

**Решение:**
```powershell
# Удалите папку data если она есть
Remove-Item -Recurse -Force data

# Запустите снова
npm run db:init
```

---

## 📞 Помощь

Если возникли проблемы:

1. ✅ Убедитесь, что Node.js установлен: `node --version`
2. ✅ Перезапустили PowerShell после установки Node.js
3. ✅ Находитесь в правильной папке: `cd "C:\Users\я\Desktop\footbol site"`
4. ✅ Выполнили все команды по порядку

---

<div align="center">

**После установки Node.js проект заработает сразу!**

SQLite не требует никаких дополнительных настроек

</div>
